{
  "opencollection": "1.0.0",
  "info": {
    "name": "Picker Production Documentation accounting shopify API",
    "version": "V1.0.0"
  },
  "items": [
    {
      "info": {
        "name": "shopify",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Handle shopify integration with push system",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "{{baseUrl}}/api/pushShopifyOrder"
          },
          "docs": "Handle shopify integration with push system"
        },
        {
          "info": {
            "name": "Handle shopify integration with push system",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "{{baseUrl}}/api/saveShopifyConfig"
          },
          "docs": "Handle shopify integration with push system"
        }
      ]
    }
  ],
  "bundled": true
}